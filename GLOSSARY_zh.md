# 术语表

本框架中所用术语的定义。它们描述的是**信任维度上的概念**——而非实现细节。工具名、阶段名、提示词和内部逻辑都被刻意省略。当某个术语的对外措辞与从业者内部的说法不同时，给出的是对外措辞。

---

## 维度

**能力 (Capability)**
*模型*的属性：智能体能否解决任务？以解决率/通过率来度量（SWE-bench、Terminal-Bench 等）。它很重要，已被这个领域的其余部分充分服务，且**不是**本框架所测量的对象。

**信任 (Trust)**
工程*过程*的属性：组织能否安全地接纳智能体产出的变更？可操作地——

> **信任 = 一个被自动接纳的 AI 生成变更，实际符合既定需求的概率。**

其测量代理是独立裁决器上的 `1 − 误受率`。每当出现「信任」一词，它都必须能还原为这一可测量属性；它从不是一种感觉。

**信任维度 vs 能力维度**
二者足够正交，是两个不同的问题。一个能力更强的模型，仍可能产出一个组织不应盲目接纳的变更；安全地接纳它是过程问题，而非模型问题。

---

## 信任如何被测量

**误受 (False-accept)**
一个被该层**接纳**（判决：accept）、但实际**并不**符合的变更——它无法通过独立裁决器（未解决问题，或令既有行为回归）。被实际接纳的误受，正是整个框架旨在减少的、代价高昂的事件。

**误受率 (False-accept rate)**
被接纳的变更中误受所占的比例。核心信任信号；`1 − 它`即信任的可操作代理。标题性的主张始终是**误受↓且解决率不变**——绝不是解决率的提升。

**误拒 (False-block)**
相反方向的错误：该层**拒绝**了（或未能确认）一个实际正确的变更。误拒之所以重要，是因为一个过度拒绝的仪器可以伪造出低误受率。排查并修复误拒，是证明仪器本身可信的一部分。

**独立裁决器 (Independent oracle)**
一个变更被对照衡量的、外部的正确性标准（例如基准的参考测试），与任何智能体或验证层可能影响的东西**相互隔离**。数字只有相对于一个系统无法为之复习的裁决器时才有意义。

**留出集评测 (Held-out evaluation)**
一个冻结的、带种子的任务集，与开发中使用的任何集合**没有重叠**，只运行一次以产出可发布的数字。开发集（「dev-set」）上的观察是反馈，不是证据——标题性的数字只来自留出集的那次运行。

**校准 (Calibration)**
调整验证，使其置信度与现实相符：一个自信的*接受*应对应一个确实符合的变更，而不确定性应以*复核*的形式浮现，而非误判为*接受*。一个未经校准的仪器会报出并不存在的信任。

---

## 判决如何形成

**独立验证 (Independent verification)**
从智能体**从未掌控**的证据形成接纳决策——而非依据智能体的自我报告。这是整个框架的前提。

**自我指涉的测试 (Self-referential test)**
智能体为自己的变更编写的测试。无论是正确的修复，还是自信地出错的修复，它都会通过，因此它不携带任何关于该变更是否可被信任的独立信号。独立验证之所以存在，正是因为自我指涉的证据并不可靠。

**对修复盲写的验收测试 (Fix-blind acceptance test)**
只依据**既定需求**编写的验收测试，编写时看不到智能体的修复（也看不到隐藏裁决器）。因为它对解法是盲的，通过它就是关于*需求*的证据，而非关于智能体叙述的证据。

**回归检查 (Regression check)**
验证变更没有破坏原本可用的行为——「符合」的后一半：一个变更可能达成了它自己的目标，却仍是误受，如果它令别处发生了回归。

**防泄漏 (Leak-safety)**
保证验证过程绝不查看或接触它被对照衡量的隐藏裁决器——它无法「为考试而复习」。没有防泄漏，再高的通过率也毫无意义。

**可复现的判决 (Reproducible verdict)**
同样的证据产出同样的决策，并附带一份说明*为什么*的可存档轨迹。可复现的决策可以被审计；概率性的意见不能。

---

## 决策

**三态决策 (Tri-state decision)**
输出是三态的，而非两态：一个「能回答就回答，否则上报」、懂得自己何时无法判断的系统。

> **接受 (accept) · 复核 (review) · 拒绝 (reject)**

- **接受 (accept)**——经独立验证确认符合。
- **复核 (review)**——就独立可检验的范围而言它是符合的，但需求没有完全确定其余部分；应由人来看一眼。（内部对这种诚实的「我无法完全背书」状态可能用别的标签；**对外**的措辞是*复核 / 需要复核 / 上报*——绝不用「证据不足」，因为该层*确有*证据；它只是仅覆盖了被明确指定的部分。）
- **拒绝 (reject)**——独立检查与该变更相矛盾（未通过验收测试，或发生回归）。

**上报/升级 (Escalation)**
当验证无法诚实地达到*接受*或*拒绝*时，刻意把决策交给人。*复核*的价值在于：它让「不确定」成为一个显式的、可追踪的结果，而非一次悄无声息的误受。

---

## 资产的三个组成部分

**信任基准 (Trust Benchmark)**
一种可复现的方法，在一个独立任务集上测量误受（及相关信任信号）。用数字回答「这个接纳过程有多可信？」。

**信任控制器 (Trust Controller)**
在工程过程中应用上述原则的接纳/判决层：独立、防泄漏的验证 → 三态判决 → 代码库。控制器可被他人实现；真正复利的部分，是围绕它的测量方法学。

**信任证据 (Trust Evidence)**
那些可复现的案例、校准与结果，它们说明*为什么这些测量值得被信任*——对照实验、防泄漏检查，以及误拒排查。信任度量本身必须可信，而这正是它被证明之处。

---

## 交叉引用

| 概念 | 在何处讨论 |
|---------|----------------------|
| 能力 vs 信任、过程原则、对反驳的回应 | [MANIFESTO_zh.md](MANIFESTO_zh.md) |
| 自我指涉的测试、独立/防泄漏验证、三态 | [MANIFESTO_zh.md](MANIFESTO_zh.md) |
| **尚未**证明什么；留出集状态；有效性威胁 | [当前范围与局限](CURRENT_SCOPE_AND_LIMITATIONS_zh.md) |
| 判决逻辑的交互式图示 | [architecture_logic_sim/](architecture_logic_sim/README.md)（英文） |
