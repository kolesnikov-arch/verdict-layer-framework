# 判决层宣言

*框架背后的推理：以独立验证取代自我报告。*


> **翻译状态 / Translation status:** 本文档翻译于 **2026-07-25**，此后不再同步更新。
> 英文版本为唯一权威版本，如有出入以英文为准。
> *Translated 2026-07-25 and frozen. The English document is canonical; where the
> two differ, the English text governs.*

## 转变

智能体已经能写代码。这个领域测量的是它们*能不能*写——解决率、通过率、基准分数。那是**能力 (capability)**，而能力是*模型*的属性。

但它不是工程组织在变更进入 `main` 之前必须回答的问题。那个问题是**信任 (trust)**：

> **能力问的是：智能体能否解决任务？**
> **信任问的是：组织能否安全地接纳智能体产出的变更？**

本文其余部分都由这一条原则展开：

> **信任是工程*过程*的属性，而非模型的属性。**

能力存在于模型之中；信任存在于变更被*如何接纳*之中。把这两者分开，正是让这项工作避开模型/智能体的战场——前沿实验室与智能体框架已在那里竞争——而停留在一个独立维度上的关键。

为了不让「信任」一词悄悄滑回「治理」，我们把它锚定在可测量之物上：

> **信任 = 一个被自动接纳的 AI 生成变更，实际符合既定需求的概率**——以独立裁决器上的 `1 − 误受率` 来度量。

这里每一次使用「信任」，都必须能还原为这一属性。先定义，再有数字，最后才有更宽泛的论断——绝不颠倒顺序。

## 为什么自评的变更不可被信任

默认的循环让智能体给自己的工作打分：它既写变更，又写为该变更背书的测试。但智能体为自己的变更编写的测试是**自我指涉的**——无论是正确的修复，还是自信地出错的修复，它都会通过。再加一个模型当「审查者」也解决不了这个问题；你只是增加了又一个有利害关系的叙述者，而非一次独立的测量。

「智能体说它能用」不是证据。判决必须来自智能体从未掌控的东西。

## 决策是三态的，而非二态

乐观的系统给出接受/拒绝两态。一个可信的系统需要第三态——一个「能回答就回答，否则上报」、懂得自己何时无法判断的系统：

> **接受 (accept) · 复核 (review) · 拒绝 (reject)**

那个诚实的**复核**——「就独立可检验的范围而言它是符合的，但需求没有完全确定其余部分，因此应由人来看一眼」——是特性，不是弱点。一个可信赖的*是*（低误受），值得用更多诚实的*我无法为此背书*作为代价。

## 独立的、防泄漏的验证

判决是基于智能体既未编写、也无法影响的证据形成的：

- 一份**对修复盲写的验收测试**——只依据既定需求编写，而非依据智能体的解法；
- 一项**回归检查**，确认变更没有破坏原本可用的东西；
- 并且该验证**绝不接触它被对照衡量的隐藏裁决器 (oracle)**——它不能「为考试而复习」。正是这种防泄漏性，让得出的数字有意义。

判决是**可复现的**：同样的证据产出同样的决策，并附带一份说明*为什么*的可存档轨迹。可复现的决策可以被审计；概率性的意见不能。

## 为什么是现在

随着开发自主性上升，每个变更上的人工检查数量随之下降——审查不再是完整的检查，而变成一种抽样过程。于是，单个被悄悄接纳的错误变更，其代价的增长速度，**快于**生成代码的成本。市场正走向更多 AI 编写的代码*以及*更低的人工控制密度——恰恰是在这种条件下，独立的接纳决策从可选变为承重。

## 对常见反驳的回应

**「下一代模型会解决这个问题。」** 这要从架构层面、而非经验层面来回答：即便未来的模型降低了绝对错误率，组织仍然需要一个*独立的*接纳决策。对一个变更的信任，不能被完全委托给产出它的那个系统——这是一项结构性要求，而非模型质量上的暂时缺口。

**「这不就是 CI / linter / 策略引擎吗？」** 那些都有价值，也都不是我们的主张。我们的主张是：对 AI 生成变更的一种**独立、防泄漏、经过校准**的验证，其判决建立在智能体从未掌控的证据之上——并且，在一个独立的集合上，以它*削减了多少被实际接纳的误受*来度量。

## 信任度量本身必须可信

这是最深的一点。测量仪器也会说谎——如果它未经校准，或泄漏了答案，它就会报出并不存在的信任。所以，在任何基于它的数字被发布*之前*，仪器本身必须先被验证：校准、防泄漏，以及对**误拒 (false-block)**（仪器错误地拒绝了一个正确变更的情形）的刻意排查，都是证明的一部分，而不是杂务。一个隐藏自身测量误差的信任框架，已经背叛了它自己的论点。

## 状态——以诚实为构造前提

这是一项进行中的研究工作，局限先行：

- 标题性的数字来自一次**冻结的、独立的留出集 (held-out) 评测**，已于 2026-07-05 依照预注册计分契约完成：**无门控 17/50 静默交付的错误修复，有门控 0/50**——完整结果与可复现工件见 [patchward（中文版）](https://github.com/kolesnikov-arch/patchward/blob/main/RESULTS_zh.md)。开发过程中的观察仍然只是观察，不构成证据。
- 目前仅在一个公开的软件工程基准裁决器上验证，而**非**生产环境的 CI。
- 在得出任何结论前，请先阅读 **[当前范围与局限](CURRENT_SCOPE_AND_LIMITATIONS_zh.md)**。

## 一句话

> 智能体已经能写代码。尚未解决的问题是**对变更的信任**。本框架描述了一个独立的、不可被操弄的层，它测量的不是「智能体有没有解决它」，而是「当智能体自信地出错时，能否被抓住」——并且，在一个独立的、预注册的留出集上，它把静默交付的错误修复从 **17/50 削减到 0/50**，全部代价随结果一并公开（[结果，中文版](https://github.com/kolesnikov-arch/patchward/blob/main/RESULTS_zh.md)）。

*（N 来自留出集评测；在它存在之前，它只是一个占位符。）*

---

*智能本质是概率性的。而接纳决策必须是独立的、可复现的，并对它尚不能验证之物保持诚实。*
